# 项目结构表

本项目统一使用 LiteLLM 对外入口，号池负责实际账号的选择和约束。LiteLLM 与号池 Manager 保持两个进程，分别管理请求和容器生命周期

| 目录或文件 | 职责 | 何时修改 |
| --- | --- | --- |
| `litellm/` | 模型 SDK、供应商协议、路由和计价 | 接入模型、调整协议或价格计算 |
| `litellm/proxy/` | HTTP / WebSocket 入口、鉴权、防护栏、用量和管理 API | 调整统一入口与后台管理 |
| `litellm/proxy/auth/` | 虚拟密钥、模型权限、预算检查 | 调整访问权限 |
| `litellm/proxy/management_endpoints/account_pool_integration.py` | 卡片密钥接入、卡片范围、内部转发签名 | 调整两层服务之间的请求身份 |
| `litellm/proxy/management_endpoints/account_pool_gateway*.py` | 校验内部转发、申请租约、执行请求和释放租约 | 调整号池转发与并发处理 |
| `litellm/proxy/management_endpoints/account_pool_reconciler.py` | 将卡片模型同步为 LiteLLM 受管模型 | 调整模型同步、别名与可用状态 |
| `litellm/proxy/management_endpoints/account_pool_request_log.py` | 请求用量、缓存命中、正文采集及成本估算 | 调整号池请求摘要 |
| `litellm/proxy/management_endpoints/account_pool_full_logs.py` | SQLite + gzip 完整日志、筛选和保留期限 | 调整完整对话存储 |
| `account-pool/account_pool/` | 独立 Manager，管理卡片、凭证、配额、策略及容器 | 调整号池业务 |
| `account-pool/account_pool/channels/` | CLIProxyAPI 等渠道的授权、模型发现和配额接口 | 修复渠道及供应商问题 |
| `account-pool/account_pool/gateway_service.py`、`gateway_repository.py` | 校验卡片规则、数据库租约、并发及冷却 | 调整账号准入和路由约束 |
| `account-pool/account_pool/credential_ownership.py` | 凭证独占绑定 | 调整卡片与凭证关系 |
| `account-pool/account_pool/management_repository.py` | 日常日志、筛选和统计 | 调整日志查询 |
| `ui/litellm-dashboard/` | 管理网页 | 修改界面 |
| `ui/litellm-dashboard/src/app/(dashboard)/account-pool/` | 号池卡片、设置、认证文件和日志页面 | 修改号池界面 |
| `ui/litellm-dashboard/src/lib/http/schema.d.ts` | 根据 OpenAPI 生成的前端类型 | 后端接口变化后生成，不能手改 |
| `schema.prisma`、`litellm/proxy/schema.prisma`、`migrations/` | LiteLLM 数据库结构及迁移 | 修改虚拟密钥、模型、用量等数据结构 |
| `account-pool/account_pool/repository.py` | 号池独立数据库初始化与连接 | 修改号池数据存储 |
| `model_prices_and_context_window.json` | 内置模型价格与上下文信息 | 更新模型默认计价 |
| `deploy/` | 服务器 Compose、发布工具、备份与回滚说明 | 发布服务器版本 |
| `tests/test_litellm/` | LiteLLM 和集成后端测试 | 验证后端修改 |
| `account-pool/tests/` | Manager、凭证、生命周期及租约测试 | 验证号池修改 |
| `ui/litellm-dashboard/src/**/*.test.tsx` | 前端组件测试 | 验证交互修改 |

| 数据位置 | 内容 | 注意事项 |
| --- | --- | --- |
| LiteLLM PostgreSQL | 虚拟密钥、模型配置、预算及标准用量日志 | 统一成本记账来源 |
| 号池 PostgreSQL | 卡片、凭证关系、策略、租约、日常日志 | 与 LiteLLM 数据库分别备份 |
| `ACCOUNT_POOL_FULL_LOG_DIR` | 完整请求与回复，默认部署挂载到 `/var/lib/litellm/full-logs` | 含会话正文，按保留期限管理 |
| Manager 的账号数据卷 | CLIProxyAPI 环境运行数据与认证信息 | 由 Manager 管理，不能靠手动删目录解绑 |

公共请求顺序：LiteLLM 鉴权及预算权限检查 -> 防护栏与标准路由 -> 签名内部网关 -> 号池卡片准入及租约 -> CLIProxyAPI / 上游。响应经过 LiteLLM 后续处理并统一记账，号池独立保存账号操作摘要
