# 服务器验收报告

验收日期：2026-09-17，北京时间。先执行完整测试矩阵，再汇总修复并复测；本报告区分本地回归、服务器真实调用和未通过的项目

## 2026-09-17 晚间补充：流式错误与成本统计

本次修复仅在本地提交，未 push、未构建、未部署。以下补充覆盖后续排查结果，后面的服务器验收结论保留为此前的记录

线上追加的五次调用均成功，LiteLLM 标准日志与统计接口均有记录。但其中 Chat 流式请求在号池记录为输入 2877、输出 5 Token，LiteLLM 记录为输入 2597、输出 1 Token，两边金额分别为 $0.0005814 和 $0.0005206。读取已保存的完整响应确认，上游把 usage 放在 finish_reason 结束块中。OpenAI SDK 的 CompletionUsage 覆盖了 LiteLLM 已转换的 Usage，后续统计读不到字段而采用估算。现已统一转换，保留输入、输出、缓存及推理 Token

Claude Code 的 invalid_prompt 来自上游拒绝。对应三条请求各执行一次号池尝试，号池没有内部重试；客户端明确提示了流式失败后改用非流式重试。此前网关丢弃流式错误码并返回通用错误，SDK 又把它当作连接故障。现保留错误类型、错误码和状态：输出前返回 HTTP 400，输出后保留 SSE 中的 400 错误，不因该拒绝重试、切换账号或设置模型冷却。带工具请求同样先识别错误，仍禁止自动重放；上游私密报错正文不会透传

本地回归覆盖真实 Router、OpenAI SDK 和本地 HTTP 号池网关，上游使用固定响应模拟。验证输入 2877、输出 5、缓存输入 2048 Token 在客户端、号池及标准日志中一致；按测试价格计算的 $0.0002742 进入 LiteLLM 成本回调和标准日志对象。同步与异步流、独立用量块、结束块携带用量、工具请求、输出前后拒绝均有回归。首轮相关测试 1470 项通过，业务源码 Ruff 与格式检查通过

最终用量转换调整后，流式核心与号池网关、集成测试再次通过 243 项，OpenAI 流式转换测试通过 9 项。号池 SSE 模块类型检查通过；既有大型模块仍有类型诊断，本次未进行全仓类型清理

这些结果证明本地修复有效，尚不能作为新版服务器验收。上游实际拒绝仍会返回错误，客户端自行发起的新请求也不能由网关保证消除。上线后还需真实调用核对标准日志、号池日志和成本，本次未回填历史估算记录

## 验收结论

统一入口已经可以完成真实模型调用，LiteLLM 标准成本与号池记录能对应。卡片密钥生命周期、管理接口、日志开关和并发租约通过测试

目前不能判定整体验收通过。真实模型调用多次遇到上游 `server_is_overloaded`，最后一轮还出现 `refresh_token_invalidated`。流式及 Responses 的标准入口成功调用尚未验证通过，需要恢复账号认证后补测。当前证据不支持把问题归因于内存不足，也不能证明所有 token 刷新竞争都已消除

## 已整合的修复

| 问题 | 修复结果 | 验证 |
| --- | --- | --- |
| 动态模型、不限并发配置导致公共入口错误拒绝 | 注册模型时同步路由并发状态，`0` 正确表示不限并发 | 本地路由回归及服务器真实 HTTP 200 |
| 仪表盘、统计 SQL 返回 500 | 修复 SQL 百分号与数据库参数占位符冲突 | 真实 PostgreSQL 和接口读取通过 |
| 卡片密钥轮换失败 | 修复 Prisma JSON 查询及动态包装器兼容性，撤销从主库读取 | 创建、轮换、撤销和删除完整流程通过 |
| 卡片范围内无可用账号返回错误状态 | 保留卡片范围约束，区分繁忙、冷却和越权 | 冷却拒绝、越权拒绝及租约释放通过 |
| 完整日志开关误触上游配置更新 | 日志设置独立保存，上游标量设置顺序更新，跳过无变化的 YAML 更新 | 开关保存通过，失败请求正文按设置跳过 |
| 号池价格缺失、两边日志难以关联 | 合并内置目录和配置价格；HTTP 由标准链路统一记账；保留关联 ID | 成功样本两边金额相同，没有额外 `account-pool:` 计费行 |
| 繁忙冷却到期仍无法恢复 | 仅对明确的过载状态允许恢复探测，凭证错误继续阻止使用 | 状态回归及真实成功调用 |
| 认证失效仍显示可用 | 识别 `status=error` 下的认证错误，即使 `unavailable=false` 也冷却并提示重新认证 | 新增回归先失败后通过，线上复测见发布记录 |
| Responses 失败日志缺关联 ID、端点类型为空 | 统一合并两种内部元数据，并在失败回调前保留调用类型 | 真实失败处理链路回归通过，线上复测见发布记录 |

认证文件刷新、仪表盘配额刷新、卡片名称下拉框、认证文件对应账号、日常及完整日志筛选、失败正文开关均已在实际网页核对。用于辨识的完整账号信息不写入本报告

## 真实调用与成本

| 项目 | 结果 |
| --- | --- |
| 请求入口 | 普通虚拟密钥，`POST /v1/chat/completions`，经过 LiteLLM 和号池 |
| 模型 | `gpt-5.6-luna` |
| 结果 | HTTP 200，回复 `Two plus two equals four.` |
| 用量 | 输入 314，输出 10，缓存输入 0 Token |
| 配置价格 | 输入 $0.0000002 / Token，输出 $0.0000012 / Token |
| LiteLLM 标准成本 | $0.0000748 |
| 号池成本 | $0.0000748，缓存率 0%，完整日志已存储 |
| 标准记录 ID | `resp_0036e7c7cf9ea027016aabd31d781887d0b23c8ec6b6d7b2e7` |
| 号池请求 ID | `4b7f15e1-f1b0-4664-bf8f-111f10a44a5f` |
| 两边关联字段 | `metadata.spend_logs_metadata.account_pool_request_id` |

该成本为配置单价乘以 Token 数的估算，不是 Plus 订阅的实际扣费。两处金额不能相加。此次成功样本缓存为 0，不能用它证明非零缓存场景的真实上游计价

此请求耗时 61.9588 秒，期间经历上游过载与重试；标准日志记录的 LiteLLM overhead 为 9.971 毫秒，该指标不是整个号池网关的全部耗时。内部直连诊断曾得到普通请求 1.7571 秒、流式首字 1.5259 秒和总时长 1.7158 秒，但这些绕过标准入口的结果不能算作标准记账与公共流式验收

同批公共入口的两次串行、Chat 流式、Responses、Responses 流式和固定卡片密钥请求均遇到上游过载，耗时约 4.4 至 88.4 秒。补充两个模型以及直接请求 CLIProxyAPI 也出现上游错误，说明延迟包含上游及重试影响，不能仅归咎于网页或 LiteLLM

## 并发与凭证

| 测试 | 结果 | 能证明什么 |
| --- | --- | --- |
| 6 个内部诊断请求，临时设置卡片上限 3，同时刷新配额 | 租约最高 3，结束 0；1 成功、5 失败 | 上限执行且租约释放，没有遗留占用；不能证明上游能稳定承载 6 并发 |
| 3 个公共请求，同时刷新配额 | 200 / 401 / 503 各一次；租约最高 2，结束 0；配额刷新 200，4.1631 秒 | 成功请求经过标准计费，认证失败后的请求被拒绝，结束没有残留租约 |
| 卡片设置恢复 | `plus02` 并发设置恢复为原值 0，其他原有卡片启用状态保留 | 测试用限制未遗留 |
| 同凭证管理请求 | 同一凭证管理调用加锁串行，不同凭证仍可并发 | 本地回归覆盖管理接口互斥 |

19:47 左右，CLIProxyAPI 的三次 token 刷新重试返回 `refresh_token_invalidated`，随后请求返回 `auth_unavailable`。这一轮未发现 `refresh_token_reused`，两种错误不能混为同一结论。当前未查明此次凭证失效是否与并发有关，也没有证据说是订阅到期或额度耗尽

现有锁保护 Manager 发起的同凭证管理接口调用，不覆盖 CLIProxyAPI 进程内部所有模型请求和后台自动刷新路径。要排除这部分竞争，需要在恢复认证后针对 token 到期进行受控复测，必要时修改或升级 CLIProxyAPI 自身的刷新互斥。没有为了测试删除真实凭证或绕过卡片约束

凭证独占、卡片删除、先删凭证后停止调度、清理失败保留归属等有本地回归覆盖。线上只删除了临时卡片，没有删除真实账号凭证，因此不声称真实账号的所有破坏性清理路径均已实测

## 密钥与日志

临时卡片密钥创建 200、轮换 200；旧密钥立即返回 401，新密钥查询模型返回 200；撤销返回 204，撤销后返回 401；删除临时卡片返回 204，标准临时密钥清理返回 200。全部测试用密钥已撤销，历史日志保留

无效密钥返回 401、超预算返回 429、越权模型返回 403。普通虚拟密钥先接受 LiteLLM 规则检查，路由进入号池后继续接受卡片规则约束；固定卡片密钥不能跨出绑定范围。真实付费防护栏供应商、成本优化的每一项策略没有逐一启用测试

打开“失败请求不保存完整日志”后，一次失败请求保留 5 条日常尝试摘要、0 条完整正文。开关后续复测开、关均保存成功，最终恢复原值 `false`。此开关只影响号池完整正文，标准错误日志和日常摘要仍记录，历史正文未清除

## 页面接口与资源

在真实模型并发期间，8 类接口各请求 8 次、客户端并发 4，共 64 次全部返回 200

| 接口 | 中位耗时 | 最大耗时 |
| --- | ---: | ---: |
| 模型列表 | 53.3 ms | 116.7 ms |
| 模型和端点 | 71.7 ms | 98.9 ms |
| 卡片列表 | 172.9 ms | 342.1 ms |
| 认证文件 | 114.3 ms | 188.0 ms |
| 号池仪表盘 | 324.6 ms | 337.2 ms |
| 号池统计 | 168.8 ms | 203.1 ms |
| 日常日志 | 118.4 ms | 120.9 ms |
| 完整日志 | 83.8 ms | 266.5 ms |

这些为服务器本机 HTTP 耗时，不包括用户网络和浏览器渲染。测试期间 SSH 隧道发生断线，浏览器的连接拒绝不能当成服务端页面慢的证据

服务器内存总量 7270 MiB，主要调用测试期间使用约 3107 至 3163 MiB；最后部署启动时达到 3394 MiB，收尾时使用 3232 MiB、可用 4037 MiB。收尾时 LiteLLM 使用 1002 MiB，Manager 使用 96.21 MiB。未启用 Swap，LiteLLM 与 Manager 未出现 OOM 或容器异常重启。该负载下没有内存耗尽证据，增加内存不能直接解决上游认证和过载错误；尚未完成长时间高并发容量测试

## 测试范围与限制

最新本地 Manager 454 项、日志和成本相关 277 项通过；此前密钥仓储与请求路由 84 项、号池前端 118 项等回归通过。计数存在不同批次重叠，不能相加当作独立测试总数。新增两个回归在修复前失败、修复后通过，变更文件通过 Ruff 与差异格式检查

扩大仓储测试时 149 项通过、4 个既有用户计数测试失败，本地未生成完整 Prisma 客户端；全仓前端类型检查存在既有测试错误。服务器上的真实 Prisma 密钥查询、数据库计价、密钥轮换及撤销已单独验证。没有声称全仓所有测试通过

尚未通过或未完成：公共流式及 Responses 的真实成功调用、重新认证后 token 到期并发复测、真实凭证删除清理、所有防护栏和成本优化策略、长时间容量与用户端网络性能测试

## 发布与留档

已提交并推送中文历史：`8efc96a901`、`5c4f44643b`、`cbff80253e`、`4a0a2127a5`。通过配套镜像部署流程自动备份再切换，数据库和认证文件保留；发布任务及配套镜像备份可在版本管理查看

20:24 部署任务 `c88ba3fb0d45ac245ae616b9eae742f1` 成功，LiteLLM 与 Manager 的实际镜像标签均为 `4a0a2127a58217572453853e01d215c8c7356c06`，两个健康检查返回 200。源码已拉取，部署 `.env` 的版本同步为 `4a0a2127a5`，独立发布服务继续固定在原版本 `11341345b3`

最终只针对本批修改补测：刷新后 `plus02` 为 `cooling_down`、自动冷却开启，上游认证状态为错误。由于上游错误文本已从结构化错误变为 `unauthorized`，当前卡片通用 `last_error` 为空，结构化认证错误提示由回归测试覆盖，不声称本次线上出现了该提示

固定卡片测试密钥请求 `/v1/responses` 返回 429，耗时 0.2065 秒；这次在 LiteLLM 无可用部署检查处拒绝，没有进入号池上游执行。标准失败日志仅一条，类型为 `aresponses`、费用与 Token 均为 0，关联 ID `d3b7b6da-13f2-414e-8035-f68150205358` 与响应头一致。它验证失败日志修复，不代表 Responses 成功验收，也不会产生一条实际账号执行记录

收尾时租约 0、临时测试密钥已清理，完整日志开启、失败不保存正文开关恢复为原值 `false`；原有三张卡片保留。文档后续提交只更新验收与使用说明，运行镜像仍以上述代码提交为准

服务器受限目录 `/root/litellm-audit-20260917` 保留本次详细输出。需要恢复认证后补测公共普通及流式 Responses、Chat 流式，以及凭证临近到期时模型请求与配额刷新的重叠场景；本轮停止继续请求已失效凭证，避免无效重复调用
