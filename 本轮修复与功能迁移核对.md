# 本轮修复与功能迁移核对

## 2026-09-18 密钥查看、认证占用与请求诊断

上线验证发现并补修额度恢复问题：模型旧快照的 5 小时窗口虽已重置，仍以剩余 0 拦截请求。原生 Router 和最终网关现在按窗口截止时间排除过期值，保留尚未到期的周额度限制；过期数据不冒充新额度，配置了额度保留阈值时仍要求有效快照。认证返回纯文本 unauthorized 时明确显示需要重新认证

虚拟密钥列表、详情、新建和重新生成结果默认显示星号，提供显示、隐藏及复制按钮。再次查看通过单独的 `/key/reveal` 接口授权，管理员和密钥所属用户可以访问，只读用户被拒绝。明文不进入列表返回、浏览器持久存储和推理鉴权，服务端使用现有加密配置保存密文；关闭页面、切到后台或显示 30 秒后隐藏。旧密钥只有哈希，无法恢复原文，不自动重新生成，也不影响原调用

数据库新增 `LiteLLM_VirtualKeySecret`，与原生虚拟密钥关联，删除和更新哈希级联处理。创建及重新生成密钥时同时写密文；未配置独立盐值时，主密钥轮换也更新此密文。迁移只新增表，不改写已有密钥或用量数据

认证文件上传重复误报的原因是替换后仍保留历史凭证占用。成功读取唯一认证文件并完成模型校验后，仅保留实际凭证的指纹；校验失败时保留已有占用，避免错误释放真实账号。修复同时保留当前凭证重复检查和有凭证卡片的明确替换确认

原生日志详情新增阶段耗时，保存最近 7 天的解析、并发租约、上游响应头、转发与输出、释放及入口总耗时。后台写入独立 SQLite 文件，失败不阻断请求；不依赖完整对话日志开关，阶段可能重叠，不能相加作为总耗时。原生费用仍写 SpendLogs

Router Settings 增加按指定卡片顺序选择，列表直接获取账号名称与模型数量，可调整顺序。同优先级内先经过密钥权限、可用性和原生重试过滤，再应用顺序，未列出卡片排在末尾。全局总览增加设置生效规则。旧单卡自定义顺序值仍保留，不自动覆盖新的全局设置；有状态续接、已开始输出的流和工具请求不跨账号自动重放

本地验证覆盖认证占用释放、失败时保留占用、密钥加解密与权限、卡片顺序保存和请求阶段持久化。全仓 TypeScript 和旧 Python 大文件仍有既有类型错误，本次修改的前端源文件没有新增类型错误。实际部署、普通和流式调用及耗时结果以交付记录为准

## 2026-09-18 后续修复与卡片总览

全局设置总览改成自适应卡片：顶部显示配置版本、保存与同步情况、数据来源；每组展示当前值、作用范围与修改入口。开关显示状态标签，复杂规则折叠展开，接口读取失败明确提示。仪表盘“可用卡片”只计算就绪、启用、模型可用且配置已同步的卡片

LiteLLM 原生 Router 新增 `account_pool_routing`，支持同优先级候选卡片按额度、套餐或到期时间选择，也可继续使用原生负载均衡。保留原生权限、模型范围、order、weight、冷却与密钥卡片限制。Manager 在后台同步额度与冷却快照，单次选卡不额外查询 Manager，也不因额度刷新改写模型数据库

原生会话亲和性可使用客户端会话标识，缓存按密钥与模型隔离；TTL 可配置。原生回退排除已失败部署后，再执行亲和性，避免重新选回已失败卡片。Responses 续接及加密内容会保留原部署约束，卡片不可用时不能冒充成功续接

上游返回 HTTP 429 或流式首段额度错误时，统一网关立即交回 LiteLLM，不在同一卡片等待后再试。`simple-shuffle` 的原生 `enable_weighted_failover` 开关决定是否尝试同模型其他部署；其他策略继续使用各自原生回退机制。已输出流式内容、工具结果、工具调用与有状态请求不自动重放

加入脱敏阶段耗时日志 `account_pool_timing`，包括解析账号、获取并发租约、等待上游响应头、转发与输出、记账释放、请求总耗时。上游阶段包含账号环境及代理网络，不能据此单独断言供应商或出站代理慢；部分时间段重叠，不能直接相加。日志级别为 INFO

旧单卡策略值仍保留。旧权重、优先级、备用标记已在首次同步转换到原生模型参数，后续以原生编辑值为准。旧选卡策略与每卡会话开关不自动覆盖新的全局 Router 设置；旧自定义账号顺序尚未等价迁移。切换项目、切换预览模型继续禁用，以免暗中改变用户请求模型。供应商设置、额度、代理、OAuth、认证文件和更新回退保持现有归属；日志、成本、密钥、护栏使用 LiteLLM 原生模块

本轮验证包含双卡普通请求在首卡额度不足后切换、指定卡片与工具请求不越界、会话隔离与续接限制、快照更新不触发模型重载、Manager 导出真实额度与策略、首段流式错误、日志脱敏，以及卡片页和路由设置保存。真实多卡故障切换仍受线上可用卡片数量限制。发布后的真实请求与耗时以交付结果为准

以下为上一轮修复记录，保留用于对照

对照旧版 `11341345b3` 与当前整合版 `4e671cb394`，本轮修复统计查询、旧 Key 接口别名权限、卡片原地编辑及设置导航。页面迁移不代表旧路由策略已经全部等价实现

## 故障与修复

| 问题 | 确认原因 | 本轮处理 |
| --- | --- | --- |
| 仪表盘统计 503 | Python 运行时 Protocol 检查无法识别动态 Prisma 包装器；SQL 时间参数被 Prisma 绑定为文本 | 保留静态类型约束，调用原有读库包装器；显式将参数转换为 UTC 时间。已用服务器真实数据库读取历史调用和费用 |
| 旧 Key 在部分客户端无法获取模型 | 同一旧 Key 的 `/v1/models` 返回 200，而 `/models` 被旧 allowed_routes 拒绝 | 只对有效旧号池绑定兼容已授权端点的版本别名；不修改数据库权限，不扩大原生显式路由权限或卡片范围 |
| 卡片配置跳转离开仪表盘 | 卡片按钮被改为模型页跳转 | 运行配置、账号策略复用共享弹窗；保存后刷新卡片，密钥和日志入口仍到原生页面 |
| 模型页面自动弹出设置 | 历史 account_policy 参数触发弹窗 | 不再自动打开；默认模型列表，支持明确 tab 参数 |
| 全局配置分散难以查看 | 缺少汇总与精确导航 | 号池增加只读总览，读取运行配置、LiteLLM Router 和全局设置；展示卡片命名覆盖及未生效旧值，链接定位对应页签 |
| 原生通用设置显示已存储但请求可能失败 | 保存和重置没有等待异步接口返回 | 成功返回后再更新状态，失败显示错误 |

已确认的慢请求样本：公开域名总耗时约 23.9 秒，LiteLLM 记录约 22.3 秒，账号环境处理约 22.1 秒。主要耗时在账号环境及上游链路；这不能证明供应商网络延迟已经解决，也不能归因于页面布局。本轮没有盲目提高重试次数

## 旧模块归属与保留字段

| 原模块 | 当前位置 | 处理与限制 |
| --- | --- | --- |
| 仪表盘、卡片 | 号池仪表盘 | 保留账号健康、真实额度、启用模型、并发、代理及原地设置；统计来自 LiteLLM SpendLogs |
| 提供商 | 号池提供商 | 保留供应商、认证方式；新增账号默认并发、自动发现模型放在本页 |
| OAuth | 号池 OAuth | 保留授权及继续授权 |
| 认证文件 | 号池认证文件 | 保留导入、认证与定时刷新配置 |
| 配额 | 号池配额 | 保留真实供应商额度、刷新与卡片阈值；Token 用量不替代供应商额度 |
| 代理 | 号池代理 | 保留节点、网关、卡片代理绑定和切换 |
| 上游同步、版本管理 | 号池对应栏目 | 保留升级、兼容检查、镜像备份和回退；运行插件列表在版本管理 |
| 密钥 | LiteLLM 虚拟密钥 | 新建只走原生接口，卡片列表选择、模型权限、预算、护栏、限流沿用原生；旧号池 Key 保留兼容 |
| 日常、完整日志 | LiteLLM 日志 | 请求日志、运行日志、完整日志、保留时间及文件参数统一到原生入口；费用仍写原生 SpendLogs |
| 全局模型访问 | 模型与端点 / 模型别名 | 保留供应商 OAuth 排除模型、别名、请求范围错误规则及命名配置 |
| 全局网络 | 模型与端点 / 重试设置 | 保留默认出站代理、超时及命名配置；重试以原生配置为准 |
| 流式 | Router Settings / 流式传输 | 保留全局开关、按卡片命名配置 |
| 请求参数 | Router Settings / 请求参数 | 保留 default、default-raw、override、override-raw、filter 与按卡片命名配置 |
| 高级设置 | Router Settings / 通用 | 保留插件启用、WebSocket 鉴权、强制模型前缀及命名配置 |
| 通用护栏 | LiteLLM 护栏与密钥/模型设置 | 使用原生功能；供应商 sensitive_words 仍是供应商参数，不等同于全局护栏 |
| 全局总览 | 号池 / 全局设置总览 | 本轮新增，只读展示当前值与配置入口 |

原单卡运行配置保留名称、启用、并发、手动冷却、出站代理、启用模型及额度详情。原账号策略保留标签、组、模型排除与别名、额度保留百分比、快照最大年龄、Token 预算与周期、图像、WebSocket、超时和调试日志

供应商特有参数保留：Codex 身份指纹、CLI 限制、app-server 客户端、compact、身份混淆和 cloaking；Claude 指纹、cloak 和系统消息重建；Kimi 指纹；xAI 搜索注入；Antigravity 敏感词、签名缓存与严格校验绕过。供应商参数由现有同步器写入运行配置或认证文件；网关侧策略另由请求链路执行。UI 显示同步状态，但特定供应商是否支持仍取决于其实际版本

## 尚未等价实现的旧设置

旧权重、优先级、备用标记在首次同步时初始化原生 weight/order，之后以 LiteLLM 模型编辑为准。旧策略的 auto、priority、random、quota、plan、expiry、custom 不再作为跨卡选卡规则；部分仍映射到卡片内部 CLIProxyAPI 的凭证排序。旧 preferred_account_ids、会话亲和性及 TTL 未等价迁移，原值保留并可查看

旧 max_attempts、backoff_ms、retryable_statuses、fallback_enabled 与全局 request_retry、max_retry_credentials、max_retry_interval 不再决定当前请求重试。原生重试策略传到内部网关，已有流式输出、工具调用和有状态 Responses 不盲目重放。供应商隐式重试保持关闭

“额度不足切换项目”和“切换预览模型”仍存有旧值，但运行时强制关闭，当前没有等价实现。本轮取消可修改开关并明确展示未生效状态。启用隐式切换可能改变实际模型和重放行为，不以恢复开关的方式冒充功能兼容

## 发布前验证

后端 465 项定向测试通过，覆盖鉴权、卡片范围、流式、模型同步及统计包装器。Manager 133 项测试通过，覆盖配置、供应商同步、同步失败及网关限制。前端 25 项定向测试通过，涵盖卡片内策略保存、切换运行配置、原地保存刷新、设置总览、链接定位与旧值保留。真实服务器 Prisma 查询已验证时间参数绑定；可用卡片的配置版本与执行版本一致，运行文件确认插件启用、上游重试关闭及首帧缓冲关闭

Ruff 和本轮后端文件的 basedpyright 通过；前端 ESLint 无错误。全库 TypeScript 仍有既有测试类型错误，本轮修改文件未出现类型诊断。Windows 没有 make，未执行 make check。真实公开域名请求、生产新镜像和浏览器验收结果以本轮最终交付报告为准
