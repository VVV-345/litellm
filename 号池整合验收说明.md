# LiteLLM 原生模块迁移验收，2026-09-18

本次以 b92813bffb 为基础，将日志和账号专用密钥管理迁入 LiteLLM 对应页面，删除号池中的重复页面。卡片仪表盘、供应商授权与设置、额度读取和刷新、账号策略、出站代理、插件、更新和回退继续保留。本次提交不包含生产部署

## 功能归属

侧边栏「日志」现在包含请求日志、运行日志、完整日志、日志设置以及原有审计和删除记录。运行日志保留时间、供应商、实际账号、请求 ID、HTTP 状态、失败分类、重试链、导出及清理功能；完整日志保留输入、提示词、回复、工具调用、会话顺序、用量和清理功能。正文只在展开时读取，关闭正文采集不会删除历史记录

原生请求日志支持按账号筛选，标准请求详情按 account_pool_request_id 关联调度尝试和完整正文，同时兼容嵌套 spend_logs_metadata。卡片的日志按钮跳转到原生日志页并携带账号条件。号池日志 Tab、嵌套的原生日志页、日志配置区和重复导航已删除

日志 API 归入 /logs/operations、/logs/full 和 /logs/settings，原 /account_pool/logs、/account_pool/stats、/account_pool/full-logs 查询入口已移除。新增接口仅允许代理管理员访问，响应禁止缓存。内部沿用现有 Manager 运行记录和完整日志存储，历史数据不搬迁，也不删除。日志配置通过版本校验合并保存，只覆盖日志字段，保留供应商、额度、代理和账号策略

侧边栏「虚拟密钥」增加账号专用密钥入口，创建、重置、撤销后刷新标准密钥列表。卡片只保留带账号跳转的操作按钮，号池专用密钥弹窗与传输代码已迁出。预算、护栏和费用继续使用 LiteLLM 原生功能，项目中没有额外的号池护栏实现需要另迁

供应商额度继续由供应商适配器读取，不能用 Token 消耗代替。账号授权、供应商配置、额度窗口、刷新周期、模型范围、代理配置、指纹、并发、冷却、会话绑定、权重和优先级的保存链路保留

## 流式修复

Chat Completions 与 Responses 在没有重试候选或请求不可安全重放时，立即转发开始事件，不再等待首个内容块的缓冲。允许重试的请求仍在首段输出前保留切换能力，工具调用、已有状态以及已经输出内容的请求继续禁止重放

Responses 错误在号池网关和 LiteLLM 外层都保留顶层 type、message、code、param 与递增的 sequence_number，同时保留兼容的 error 对象。此前只修复内层，外层重新包装仍会丢掉事件类型，这次补齐外层处理。连接中断使用相同事件结构

response.incomplete 保留原始终止事件、未完成原因和用量，不再替换成通用上游错误。完整日志仍标记未完成，避免把部分输出显示为完整回复

## 实际服务器观察

本轮只读核对确认，服务器 LiteLLM、Manager、Worker 运行 b92813bffb8e9aff8faa9a23330bb01102b24632，容器健康

使用真实上游的小请求中，Chat Completions 约 2.29 秒开始分段输出，约 2.52 秒收到 [DONE]。一个 Responses 请求约 3.73 秒收到开始事件、3.80 秒收到内容，随后约 4.05 秒返回上游 invalid_prompt，且外层错误缺少 type，复现了本次包装缺陷。这些测量来自已部署旧代码，不能当作新版成功率或性能提升的证明

日志还存在上游 server_error、超时和客户端断开。一次带 reasoning_effort 的 Chat 请求被现有模型参数校验拒绝。这些独立问题没有通过隐藏异常、改写数据库或放宽上游限制来处理，页面整合无法保证供应商永远可用

## 验证与操作路径

网关、完整日志、真实 Router 集成与日志权限/设置共 149 项后端测试通过；外层流式错误 2 项及原生日志账号过滤 1 项通过。前端针对日志页面、完整正文关联、权限、配置保存、账号筛选、专用密钥及保留的账号操作共 100 项定向测试通过。未运行全量 UI 测试

本地真实 Next 页面使用隔离的模拟管理 API 验证：进入 /logs，切换运行日志、完整日志、日志设置；修改失败记录开关并保存，确认成功反馈；进入 /api-keys，选择账号打开专用密钥管理。正文按需加载、会话与清理、标准请求关联通过组件集成测试验证。模拟数据的页面验收不代表生产接口已经部署新版

改动的 Python 源码通过 Ruff，新增日志路由和流解析模块通过 basedpyright。前端变更通过 ESLint，无新增错误，保留已有复杂度等警告。全库 TypeScript 仍报告原有测试文件错误，变更源码未报告类型错误；本机未执行完整 make check

## 上线边界

需要等待本提交镜像构建成功后，拉取对应明确版本更新 LiteLLM，再验证原生日志接口、专用密钥及真实流式调用。此次未变更 Manager 存储结构和 Worker，不需要删除已有日志或重建数据。不要在新版前端继续调用已删除的旧日志路径

完整日志采集仍覆盖原有号池调用范围，其他供应商沿用 LiteLLM 自身的正文日志配置。WebSocket 沿用原链路，未宣称完成此次专项验收。上线后用真实 API Key 检查普通调用、流式首段、结束事件、工具调用、断流、不重放、额度不足、代理切换及标准费用记录，再判断实际稳定性
