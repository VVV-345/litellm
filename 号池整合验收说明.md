# 号池与 LiteLLM 整合验收，2026-09-18

本次在现有标准入口上完成共享统计和日志归口、流式错误修复以及单层重试。卡片仪表盘、供应商授权与设置、额度读取和刷新、账号路由策略、出站代理、更新和回退入口保留。本次提交不包含生产部署

## 功能归属

LiteLLM 标准入口继续负责 API Key 鉴权、预算、护栏、请求日志和费用记账。号池仪表盘改为读取 LiteLLM 标准调用记录，展示近 30 天结果，并区分数据读取失败与零请求。卡片总览保留已删除账号的历史请求汇总。账号切换后通过内部响应头关联最终账号，避免将用量归给最初选中的账号

号池日志默认复用现有标准日志组件。账号运行记录保留调度、重试、账号切换和故障细节；完整请求记录保留原有开关、存储与访问限制。卡片日志按钮继续打开指定卡片的运行记录。虚拟密钥、护栏、用量和标准日志均提供现有 LiteLLM 页面入口

供应商额度不由 Token 消耗推算，继续使用原有供应商适配器读取。授权信息、额度窗口、刷新周期、代理配置、指纹设置、并发限制、模型范围、会话绑定、权重和优先级的字段及保存链路保留

## 本次修复

Responses 流中的上游错误原先被改写成缺少顶层 type 的 SSE，LiteLLM 因而可能把错误当普通事件，客户端未收到正确异常，标准失败日志也可能缺失。现在输出有类型且经过脱敏的错误事件，覆盖过载、拒绝请求以及连接中断

关闭 CLIProxyAPI 的额外重试和 Codex 首帧缓冲。LiteLLM 号池适配层统一执行卡片的最大尝试次数、退避、可重试状态码与账号切换策略。只有允许重试且仍有候选账号的请求需要输出前缓冲；Responses 的其他请求直接转发开始事件。工具调用、已有会话、previous_response_id、后台任务仍不重放；已有输出后的流也不重放。高级设置中的旧供应商重试参数保留存储值，并明确说明当前由卡片策略接管

版本管理原先只比较 Prisma 与 SQL 表结构，会漏掉数据库中保存的 JSON 配置和枚举变化。现在兼容性指纹也检查模型字段、枚举和类型别名。旧备份需通过新版重新扫描以更新兼容性指纹，复用已有镜像归档。检测到不兼容时会阻止直接切换，保留恢复点

## 服务器日志诊断

2026-09-18 凌晨只读检查确认：上游存在过载、server_error、invalid signature、请求超时，以及客户端中断；不是单纯仪表盘布局问题。抽样中上游耗时约 32.721 秒，号池耗时约 32.966 秒，因此仍有大部分等待来自上游

抽样时段 00:29 至 00:46，号池记录 19 次请求，8 次成功、11 次失败，标准日志只记录了其中部分失败。上述流式错误修复针对这类遗漏，但不补写历史日志

旧版本 11341345b39d72b9723e4926af31691407dcdff7 也不能仅凭此前可用就认定为安全回退目标。持久化设置包含旧代码不识别的 full_log_skip_failed、日志 spend_sync_state=standard 等字段，单独回退镜像不会回退这些数据

还发现 ShadowEvalJob 字段与实际数据库形态不一致的日志回调错误、供应商返回模型名称与请求模型名称不一致，以及 GitHub 插件更新限流。它们未在本次自动改写数据库或模型映射，需分别处理，不能宣称本次整合解决所有线上错误

## 验证

管理端全量测试 460 项通过；代理侧号池相关 184 项通过，另加首帧即时转发测试通过；用量及 Responses 错误测试 217 项通过，另加最终账号记账测试通过；前端号池 74 项通过。兼容性代码最后调整后单独复验版本管理 30 项通过。以上重复运行不重复计算

新增统计 SQL 已在生产 PostgreSQL 只读事务中执行，能按账号汇总成功、失败、Token、缓存用量和成本。此验证没有写入生产数据，也没有切换服务器版本

改动 Python 文件经过 Ruff 检查与格式检查；新增统计模块和关键兼容性模块通过 basedpyright。前端变更经过 Prettier 和 ESLint，现有复杂度警告保留。全库 TypeScript 检查仍有已有测试类型错误；扩大 Python 管理端检查遇到本地 prisma.actions 缺失，相关测试无法收集。本机未安装 make，未宣称完整 make check 通过

## 上线与边界

上线需要成套更新 LiteLLM 与 Manager，并确认 Manager 启动配置同步成功，CLIProxyAPI 内部重试为 0、Codex 首帧缓冲为 false。若旧 CLIProxyAPI 配置同步失败，不能在未核对的情况下继续使用新的外层重试，以免重复尝试。版本管理 Worker 也需要单独更新，否则旧 Worker 不会执行新增兼容性检查

本次仍保留标准 Router 到号池适配层的内部 HTTP 转发以及独立 Manager，未进行协议栈整体替换。WebSocket 沿用原有链路，需要独立验收。上线后应使用真实 API Key 验证普通与流式调用、工具调用、额度不足、代理切换、账号回退、断流和标准费用日志，再根据首 Token 延迟与失败率判断实际改善

代码测试通过意味着本次改动可提交审查，不等于供应商服务始终可用。生产未部署之前，当前线上行为不会因本地提交而改变
