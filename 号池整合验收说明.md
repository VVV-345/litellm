# LiteLLM 原生功能整合验收，2026-09-18

本次接续 67d0a1fb7d，修改 LiteLLM 的密钥接口、原生模型编辑、Router 和请求配置处理，以及对应前端源码。号池不再提供另一套新建密钥和综合配置页面。本次不部署生产服务器

## 功能归属

| 功能 | 现在的位置与行为 |
| --- | --- |
| 新建密钥 | 原生虚拟密钥创建表单，管理员可从卡片列表选定一张卡片，展示该卡片启用模型；留空不限制卡片候选 |
| 护栏、预算、模型权限、限流 | 沿用 LiteLLM 原生密钥和模型设置，卡片绑定不扩大已有权限 |
| 权重、顺序与回退 | 原生模型编辑和路由策略，权重对应 weight，顺序对应 order，数值越小越优先 |
| 模型别名 | 原生模型别名页面补充供应商作用域的别名、排除模型和请求错误设置 |
| 重试与网络 | 原生模型重试页面，下面补充出站网络默认配置；请求重试读取 LiteLLM 的密钥、团队及全局策略 |
| 流式、请求参数覆盖、高级运行参数 | 原生 Router Settings 的相应配置页，支持命名配置绑定卡片 |
| 单张卡片设置 | 模型详情的卡片配置区，卡片按钮也跳转到此处；保留额度阈值、并发、启用模型、冷却、代理和供应商特有参数 |
| 日志 | 延续上一提交，原生日志中的请求日志、运行日志、完整日志和日志设置 |
| 号池 | 仅保留仪表盘、提供商、OAuth、认证文件、配额、代理、上游更新、版本管理 8 个顶层栏目 |

新卡片默认值放在提供商栏目，额度耗尽行为放在配额栏目，运行插件管理放在版本管理栏目。卡片仍展示真实供应商额度和启用模型。Token 消耗不替代供应商额度

供应商作用域的模型别名和 Antigravity sensitive_words 属于适配器参数，仍保留原有语义和存储，不冒充 LiteLLM 的全局别名或通用护栏。通用护栏直接使用 LiteLLM 现有功能

## 密钥与调用链

公开的号池专用密钥创建、状态、轮换和删除接口已经移除，新建全部走原生虚拟密钥接口。指定卡片写入服务端验证过的 metadata.account_pool_card_id，仅管理员能增加绑定；更新、批量更新和轮换均检查绑定不可修改。未绑定卡片的密钥保留全部正常路由候选，绑定后先收窄候选，再执行 LiteLLM 原有路由、权限和预算规则

旧 cpk 密钥保留兼容，已注册密钥不再额外调用 Manager 解析身份。旧密钥手动轮换使用同一个数据库事务归档旧 Token 并写入新 Token，避免被再次自动注册；事务失败时不清缓存。新建原生卡片绑定密钥支持原生自动轮换，旧 cpk 密钥需要手动轮换，且不支持宽限期

调用仍通过既有内部签名网关执行账号可用性、并发、冷却、额度和代理控制。页面配置的读取不会进入每次调用链。内部转发地址、凭证和受管模型标识不能从普通模型编辑改写，原生 weight、order、timeout、stream_timeout、护栏等字段可保存，并在同步中保留

原卡片 weight、priority 和备用状态在首次收到新版 Manager 快照时初始化原生 weight/order；之后以 LiteLLM 中编辑的值为准。旧 Manager 缺少这些字段时不会提前标记迁移完成。旧卡片策略中的跨卡选择开关不再提供第二套编辑入口

## 流式与重试

原生重试策略通过签名转发票据传入内部网关，内部完成同卡重试，SDK 和外层 Router 不再叠加重复重试。每个候选部署最多重试 4 次，同时保留原有时间预算、冷却及 Retry-After 限制。不同错误类别使用原生对应次数，400/401 不在内部重试

工具请求、已有 Responses 状态、后台请求不重放。卡片流式响应不再套用 LiteLLM 的外层流式续写回退包装，避免内部完成后的再次重放；普通请求仍可在安全条件下使用原生 order、加权或模型回退。非卡片模型的原生流式回退行为保留

延续上一提交对 Responses 错误事件类型、sequence_number、未完成终止事件和开始事件延迟的修复。读取超时可能发生在上游已处理请求之后，因此仍不盲目重放。WebSocket 保留既有链路，不宣称其已接入本次 HTTP 重试策略

## 配置保存

新增 GET /config/runtime 和 PUT /config/runtime/{section}，按模块合并保存并校验版本。保存流式配置不覆盖代理、额度、日志和供应商字段；并发修改返回 409。公开 /account_pool/settings 的整包读写入口移除，历史、预览及回退接口保留兼容

内部继续使用现有 Manager 配置存储，未迁移或删除授权、额度、日志、代理数据。本次需要 LiteLLM 与 Manager 使用同一个提交构建的镜像，才能完成旧权重和优先级的迁移

## 验证结果与限制

定向后端累计 425 项通过，覆盖 Manager 额度与设置、网关、真实 Router 接模拟上游的流式请求、密钥创建和轮换、模型同步、配置权限及原生 Responses 回退。前端 270 项定向测试通过，覆盖虚拟密钥卡片选择、模型展示、预算护栏字段、原生模型保存、卡片操作、菜单和配置保存。未运行全量 UI 测试

变更 Python 源码通过 Ruff；新增身份配置和配置接口通过 basedpyright，受管模型同步文件相对基线没有新增类型诊断。前端 ESLint 没有错误，保留既有复杂度等警告。API 类型已通过 npm run gen:api 重新生成

全库 TypeScript 仍有既有测试文件错误。本轮发现并修复日志设置对已移动字段组件的旧导入。Windows 环境没有 make，make check 和 lint-budget-update 无法执行，未手工伪造预算基线

本地 Next 页面预览遇到浏览器连接故障，可视验收尚未完成，组件测试结果不等于浏览器验收。此次没有部署修改后的生产镜像，也没有用新版代码调用真实供应商；不能据此承诺生产成功率或延迟

## 部署后验收路径

在虚拟密钥页面创建一个不绑定卡片的密钥和一个绑定卡片的密钥，核对列表模型、实际可用模型与权限交集。在模型详情修改权重、顺序、超时与护栏，等待同步后确认仍保留。分别检查普通请求、Chat/Responses 流式开始和结束事件、工具请求、断流不重放、代理切换、额度不足与标准费用记录

需要等待对应提交的镜像构建完成后，再拉取明确版本。页面整合与测试通过不能消除供应商自身的拒绝、超时或代理网络故障，仍需以真实调用和日志判断稳定性
