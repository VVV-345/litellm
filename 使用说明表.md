# 使用说明表

| 想做什么 | 操作位置 | 规则与结果 |
| --- | --- | --- |
| 添加账号 | 号池，创建卡片并完成认证 | 认证、模型发现与健康检查成功后才可使用 |
| 绑定凭证 | 号池，卡片认证及认证文件 | 凭证独占绑定，不能用同一凭证混绑多张卡片 |
| 固定使用某张卡片 | 卡片设置，创建 API Key | 新密钥为 `sk-cpk_`，同时登记到 LiteLLM 虚拟密钥；只能调用原卡片 |
| 使用已有卡片密钥 | 原客户端保持 `cpk_` 不变 | 首次成功鉴权时自动登记标准虚拟密钥；已经删除或撤销的密钥不能自动恢复 |
| 管理卡片密钥预算、模型权限及防护栏 | LiteLLM，虚拟密钥 | 标准规则先执行，卡片自身的限制继续执行；两边都允许才能调用 |
| 轮换卡片密钥 | 原卡片设置，轮换密钥 | 旧绑定撤销、旧虚拟密钥阻止使用，生成新密钥；不使用虚拟密钥的通用轮换或自动轮换 |
| 撤销卡片密钥 | 原卡片设置，撤销 | Manager 撤销绑定，LiteLLM 同步阻止使用；调用失败时刷新页面核对状态后重试 |
| 删除标准虚拟密钥 | LiteLLM，虚拟密钥 | 该密钥不能继续调用，不删除账号或认证文件；需要新密钥时在卡片中轮换 |
| 让项目按可用模型分配账号 | LiteLLM，创建普通虚拟密钥并设置允许模型 | 标准路由选中号池模型时仍进入号池检查；普通密钥没有固定卡片范围 |
| 设置价格 | LiteLLM，模型和端点 | 配置输入、输出及缓存价格；标准用量为统一记账来源，号池展示本次上游用量与估算成本，不能将两份金额相加 |
| 设置模型别名 | 号池，卡片策略 | 可用别名同步到 LiteLLM；被排除的模型及别名不可使用，同步存在一个轮询周期的延迟 |
| 查看标准日志 | LiteLLM，日志 / 用量 | 查看密钥、用户、模型、请求结果及成本；费用仍受模型计价配置和上游返回用量影响 |
| 查看卡片执行记录 | 号池，日志，日常日志 | 查看实际卡片、尝试次数、冷却、缓存率及用量；标准日志元数据中的 `account_pool_request_id` 可关联号池请求 ID |
| 筛选日常日志 | 号池，日志，设置条件后点击应用 | 支持时间、卡片、实际使用卡片、模型、结果、HTTP 状态码、端点、会话、请求 ID 等；统计和导出使用相同筛选 |
| 查看完整会话 | 号池，设置开启完整日志，再打开完整日志页 | 仅开启后保存正文；支持模型、时间、结果、请求 ID 和会话筛选 |
| 失败请求不保存正文 | 号池，日志，完整日志，打开“失败请求不保存完整日志”并保存 | 失败及中断请求保留日常错误和用量摘要；历史正文不会因切换设置被批量清理 |
| 清理日志 | 日常日志或完整日志各自的清理按钮 | 两个存储独立清理；请核对保留期限 |
| 刷新账号状态及配额 | 号池仪表盘及认证文件刷新按钮 | 用真实接口刷新，配额不足、凭证异常和订阅到期是不同状态 |
| 先删除凭证 | 认证文件管理 | 对应卡片保留并停止接单；取消授权会话并清理认证文件，成功保存空绑定后才释放凭证归属；失败保留归属供重试 |
| 删除卡片 | 号池，删除卡片 | 由 Manager 清理对应环境与绑定；清理失败保留状态供重试，不能继续调度，历史日志按自身保留策略处理 |

卡片密钥不会因开启跨卡片回退而越过原卡片范围。需要跨账号分配时使用普通虚拟密钥，号池内部的凭证独占、配额、冷却和并发限制仍会检查

| 开发与发布事项 | 命令或配置 |
| --- | --- |
| Manager 本地启动 | 在 `account-pool/` 配置环境变量后执行 `uv run uvicorn account_pool.app:create_app --factory --host 127.0.0.1 --port 8091` |
| LiteLLM 启动 | 在根目录执行 `python litellm/proxy/proxy_cli.py --config <配置文件> --port 4000`，需要可用数据库 |
| UI 开发 | 在 `ui/litellm-dashboard/` 执行 `npm run dev` |
| 内部转发地址 | `ACCOUNT_POOL_INTERNAL_URL` 默认 `http://127.0.0.1:4000`，修改 LiteLLM 监听端口时同步修改；应指向同一实例以便关联本地完整日志 |
| Manager 连接 | `ACCOUNT_POOL_MANAGER_URL` 及至少 32 字符的 `ACCOUNT_POOL_MANAGER_TOKEN`；同时需要 `STORE_MODEL_IN_DB=True` |
| 生成接口类型 | 根目录执行 `python -m litellm.proxy._lazy_openapi_snapshot`，再在 UI 目录执行 `npm run gen:api` |
| Manager 测试 | 在 `account-pool/` 执行 `python -m pytest -q` |
| UI 号池测试 | 在 UI 目录执行 `npx vitest run 'src/app/(dashboard)/account-pool'` |
| 服务器发布 | 按 `deploy/DEPLOY.md` 和 `deploy/releasectl.py` 的备份、更新、回滚流程执行；LiteLLM 与 Manager 必须使用配套版本 |

本次代码提交与推送不等于服务器已升级。部署后应使用测试卡片分别验证普通请求、流式请求、预算拒绝、防护栏拒绝、卡片冷却和日志关联，再切换实际流量

2026-09-17 验证记录：Manager 440 项、号池后端回归 126 项、号池前端 118 项通过，另有集成及健康检查等 95 项补充验证通过。新增集成模块通过严格类型、Ruff 及类型纪律检查。接口类型已重新生成。全仓前端类型检查仍有既有测试错误，本次修改的组件及测试未报告类型错误

本地 HTTP 验证使用实际 Router、内部网关及模拟上游，覆盖普通对话、Responses、压缩、流式和上游错误重试；内部 WebSocket 验证覆盖签名、帧转发和租约释放。当前机器未启动 Docker，也未配置可用的真实 PostgreSQL 客户端和上游账号，因此尚未完成生产数据库、真实账号及服务器端到端验收
